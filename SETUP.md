# ちみHub：画像投稿版の接続手順

Cloud Storage・Blaze・Analyticsは使いません。Web画面は既存のNetlify、認証はGoogleログイン、データとWebPはFirestoreに保存します。

## 1. FirebaseのWeb設定を入れる

Firebase Console → 歯車 → プロジェクトの設定 → 全般 → マイアプリ → WebアプリのSDK設定「Config」から、`firebase-config.js` の4項目を埋めます。Web設定は公開用の接続情報であり、Admin SDK秘密鍵・パスワードは絶対に入れません。

空欄の間はデモ表示を維持し、ログイン・投稿ボタンは無効になります。既存デモの法務リンクは未完成です。外部の参加受付を開始する前に、運営者・問い合わせ先を確定した利用規約・プライバシーポリシー・投稿ガイドラインを公開してください。

## 2. Authenticationを確認

Googleログインを有効化し、Settings → Authorized domainsにNetlifyのドメインだけを追加します（https://や末尾の/なし）。Googleログインはポップアップ方式です。アプリ内ブラウザで失敗する場合はSafariなどの通常ブラウザで開いてください。

## 3. Firestoreルールを公開

Firestore Database → ルールで `firestore.rules` の内容を貼り付け、公開します。**テストモードや全員書き込み許可にはしないでください。** この手順を省略すると投稿できません。

Firestore Database → インデックス → 単一フィールド → 除外を追加：

- コレクションID：`images`
- フィールドパス：`data`
- 昇順・降順・配列のインデックスをすべて無効化

CLIが使える場合は、対象プロジェクトIDを明示して `firebase deploy --only firestore:rules,firestore:indexes --project YOUR_PROJECT_ID` でも反映できます。アプリをNetlifyへ公開しただけでは、Firestoreルールは反映されません。

## 4. 運営枠を設定（Firebase Consoleから一度だけ）

1. 接続済みのちみHubで、運営用Googleアカウントで一度ログインします。
2. Firebase Console → Authentication → Usersから、そのアカウントのUIDをコピーします。
3. Firestoreの「データ」で、コレクション `settings`、ドキュメントID `registration` を作ります。
4. フィールド `adminUid` を **string型** で作り、運営アカウントのUIDを入れます。
5. フィールド `open` を **boolean型のfalse** で作ります。文字列"false"ではありません。
6. ちみHubで「自分のプロフィール」を押すと、運営専用の0番席を確保できます。
7. 動作確認・法務ページ公開後、参加受付を始めるときに `open` を **boolean型のtrue** にします。

残り1〜5番席は先着5名です。参加ボタンを押して編集画面を開いた時点で席が確保されます。複数人が同時に参加しても、同じ席は重複確保されません。

**6人制限は投稿メンバーの制限です。** Firebase AuthenticationでGoogleログインすること自体は、7人目以降も可能ですが投稿メンバーにはなれません。Googleログイン画面のアカウント数を6件に制限する仕組みではありません。一人が複数のGoogleアカウントを使うことを技術的に判定するものでもありません。

## 5. 投稿と閲覧を確認

- 運営アカウントで名前・世界観・キャラ名を入力する。
- PNG/JPEG/WebPの立ち絵を選び、圧縮後プレビューと容量を確認する。
- 「公開する」にチェックして保存する。
- 別のブラウザ／ログアウト状態で「見る」を押し、立ち絵を確認する。
- 別の参加アカウントでは他人の投稿を変更できないことを確認する。
- 「公開する」を外して保存すると、一覧から消え、共有URLからも匿名閲覧できなくなることを確認する。
- 画像の削除・差し替え・プロフィール全削除を試す。元の絵は別途保管してください。

本番の接続テストには実際のFirebase設定・ルール公開・Googleログインが必要です。自動テストはエミュレーターのダミープロジェクトのみを使います。

## 画像の仕様と限界

- 最大4画像枠：プロフィール1、キャラ3。通常画像は1枚150,000バイト以下・長辺1000px以下。
- 一覧用サムネイルは1枚20,000バイト以下・長辺240px以下。別ドキュメントなので通常画像4枚と合わせて最大8画像ドキュメント/人です。
- 合計上限は通常画像だけで3.6MB、サムネイル込みで4.08MB（文字データやインデックスは別）。
- 入力はPNG/JPEG/WebP、12MiB以下、2400万画素以下。SVGや動画、HEICは受け付けません。アニメーションの保持は保証しません。
- 画質を下げ、必要なら解像度も下げて容量内へ収めます。細い線や小さい文字は潰れる場合があります。プレビューを見てから保存してください。
- Safari等でブラウザ標準WebPエンコードが非対応の場合は、同じサイトから必要時だけ読み込むjSquash/libwebpのWebAssembly変換器をWeb Worker内で使います。PNG/JPEGから通常画像とサムネイルを生成し、透明度も保持します。画像を変換目的で外部サービスへ送りません。変換器は非SIMD版約276KB／SIMD版約340KBのどちらかを読み込みます（転送圧縮前）。
- 変換器のダウンロード失敗等でも、条件内の変換済みWebPなら原画像を保持して保存できます。この最終フォールバック時だけサムネイルがありません。
- 同時にプロフィールと変更画像を保存するため、書き込みの途中失敗で片方だけ更新されるのを防ぎます。複数タブからの編集は最後に保存した内容が優先されます。
- Rulesで本人確認、保存枠、型、容量、自己申告の寸法を制限します。Rulesは画像をデコードしないため、実際の形式・寸法・内容の完全な検査や自動モデレーションはできません。少人数の信頼できるテスト参加者向けです。閲覧時はWebPヘッダーを確認し、ユーザー文をHTMLとして挿入しません。
- 公開画像は誰でも取得・保存できます。非公開化しても既にダウンロードされた画像は回収できません。公開閲覧のアクセス増は参加者6人でも発生します。Firebase Consoleで読み取り回数と転送量を確認してください。
- 一覧ではプロフィール画像（未設定なら最初の立ち絵）のサムネイル1枚だけ、詳細を開いた時に通常画像を取得します。リアルタイム監視は軽いプロフィールデータのみです。ブラウザの永続Firestoreキャッシュは有効化していません。

## 受付停止・退会・テスト終了

- 新規受付停止：`settings/registration.open` をfalseにします。既存メンバーの編集は可能です。
- 投稿停止：対象 `members/{uid}.active` をfalseにします。既存公開データは自動で非公開にはならないため、問題投稿はConsoleで `profiles/{uid}.published` をfalseにするか削除してください。
- 画面の「投稿データを削除」はプロフィールと既知の8画像ドキュメントを削除します。Authアカウント・メンバー・席は残ることを確認画面で通知します。
- 完全退会は運営者が画像サブコレクション、プロフィール、members、対応するseats、Authenticationのアカウントを確認して削除します。Firestoreの親ドキュメント削除だけではサブコレクションは消えません。
- 席の自動再利用はしません。席とmembersを運営者が削除すると、その枠は再募集可能になります。
- Admin SDKやConsoleはRulesを迂回できる管理操作です。設定・削除先を必ず確認してください。
- オンラインテスト終了時は受付停止と告知後、データを削除し、Netlifyをオフライン版へ差し替える運用です。

## 開発者向けテスト

### iOS変換・読み込み改善版

既存のFirestore Rules・登録データ・画像形式はそのまま利用できます。ルールの貼り直しやBlazeへの変更は不要です。GitHubの修正をマージしてNetlifyが再ビルドされた後、iPhone/iPadでページを再読み込みしてください。

- 一覧のサーバー確認済みプロフィールを30秒間、詳細表示に再利用します。直接の共有リンクや期限切れ時だけサーバー取得します。
- 自分のプロフィールはログイン後に先読みし、参加確認と並列で取得します。ログインだけで席は確保しません。
- 既存メンバーには不要な席確保トランザクションを実行しません。参加確認結果は30秒のみ再利用し、実際の保存は引き続きRulesで認可します。初回の5席読み取りは並列化しています。
- 画像はUID・画像枠・サムネイル区分・revisionでキーを分離し、最大48件・120秒のメモリキャッシュで再利用します。ディスクには永続化しません。同時読み取りは一本化します。
- 公開一覧のサーバー更新で非公開化・削除を検知すると該当キャッシュを破棄し、開いている詳細も閉じ直す案内へ切り替えます。画像差し替え・ログアウト・アカウント変更・一覧取得エラーでも必要なキャッシュを破棄します。すでに取得された情報の回収を保証するものではありません。
- サーバー読み取りは15秒でエラー案内に切り替えます。書き込み／席確保の途中で成功扱いにしたり、書き込みをタイムアウトで自動再試行したりはしません。
- Firestoreのlong-polling自動検出は有効のまま、long-polling時の応答待ち設定を5秒に調整しています。全リクエストが5秒以内になる保証ではありません。
- 以前の「15秒の原因をほぼ特定」「通常1〜3秒」の説明は実測に基づく断定ではありません。通信・認証・Firestore応答の実機計測は別途必要です。

確認手順：同じiPhone/iPadでPNGまたはJPEGを選択 → プレビューと容量を確認 → 保存 → ログアウト状態で立ち絵を見る → 閉じて再度見る → 画像差し替え・非公開化が反映されることを確認。実機Safari/Googleアプリの試験と本番投稿は運営者の端末で実施してください。

Node.js 22推奨（検証環境では24）とJava 17以上を用意し、`npm ci` → `npm test` → `npm run test:rules` を実行します。ルールテストは `demo-chimikko-test` に限定し、本番プロジェクトへ接続しません。テスト用Firebase CLIはJava 17で動くバージョンに固定しています。CLIを更新する場合はJava要件も確認してください。

`npm run build` は公開用の静的ファイルだけを `dist` にコピーします。`netlify.toml` で公開先を指定しています。既存のNetlify画面で異なるビルド設定を上書きしている場合は、コマンド `npm run build`・公開ディレクトリ `dist` を確認してください。
